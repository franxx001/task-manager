{
  "name": "task-manager",
  "version": "1.0.0",
  "description": "任务管理器 — 纯静态 PWA，Supabase + GitHub Pages",
  "main": "dev-server.js",
  "scripts": {
    "dev": "node dev-server.js",
    "start": "node dev-server.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint . --ext .js,.html --ignore-pattern node_modules --ignore-pattern docs/config.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "express": "^4.21.0"
  },
  "devDependencies": {
    "eslint": "^8.57.1",
    "eslint-plugin-html": "^7.1.0",
    "vitest": "^1.6.1"
  }
}
